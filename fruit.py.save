#!/usr/bin/env python3

import sys

words = sys.stdin.readlines()
fruits = {}
fruits = {
  "apple"= True, "banana" = True, "cherry" = True, "orange" = True,
  "pear" = True,dog
cat} apple i = 0 while i < len(words): snake if rstrip.words[i] in
fruits: cherry print(words) dog i = i + 1 cherry words =
sys.stdin.readlines() cat pear horse
